apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-user
  labels:
    app: {{ .Release.Name }}-user
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-user
  ports:
    {{- range .Values.ports }}
    - protocol: TCP
      port: {{ .port }}
      targetPort: {{ .targetPort }}
      name: {{ .name }}
    {{- end }}
