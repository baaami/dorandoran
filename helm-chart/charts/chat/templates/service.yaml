apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-chat
  labels:
    app: {{ .Release.Name }}-chat
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-chat
  ports:
    {{- range .Values.ports }}
    - protocol: TCP
      port: {{ .port }}
      targetPort: {{ .targetPort }}
      name: {{ .name }}
    {{- end }}
