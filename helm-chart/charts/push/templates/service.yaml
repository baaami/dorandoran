apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-push
  labels:
    app: {{ .Release.Name }}-push
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}-push
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}